---
//import supabase from "../../utilities/supabase";

import Layout from "../../layouts/Layout.astro";

import ListView from "../../components/ListView.vue";

// const url = new URL(Astro.request.url);
// const { pathname } = url;
// const id = pathname.split("/").pop();
// const { data: list, error: error_list } = await supabase.from("lists").select().eq('id', id).limit(1).single();
// const { data: listItems, error: error_list_items } = await supabase.from("list_items").select("*").eq('list_id', id);
---

<Layout title={"title"}>
  testing
  <!-- <ListView data={listItems} title={list.list_name} client:load></ListView> -->
</Layout>