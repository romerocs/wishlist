---
import ListMenu from "./ListMenu.vue";
import supabase from "../utilities/supabase";

const { data: lists, error: error_list } = await supabase.from("lists").select("*");
---

<ListMenu data={lists} client:load />